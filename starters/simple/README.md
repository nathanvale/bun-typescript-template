# __REPOSITORY_NAME__

Read-only Bun CLI generated from the canonical simple starter.

Source packet: __SOURCE_PACKET__

```sh
bun install --frozen-lockfile
bun run start -- --help
bun run --silent start -- status --json
bun run check
```

The generated repository owns its dependencies, lockfile, source, tests and CI.
It has no runtime dependency on the template or generator.
